import test from 'node:test';import assert from 'node:assert/strict';import {assignAdminNames,christmasCountdownText,eligible,renderSlide,weightedPick,validateSlide} from '../netlify/functions/_shared/core.js';
const base={id:'a',name:'A',headline:'HELLO',subheading:'',logo:'two-pennies',weight:1,enabled:true,starts:'',ends:'',schedule:{mon:{enabled:true,start:'16:00',end:'19:00'}}};
test('schedule uses London day and time',()=>{assert.equal(eligible(base,{day:'mon',date:'2026-09-21',time:'17:00'}),true);assert.equal(eligible(base,{day:'mon',date:'2026-09-21',time:'19:01'}),false);assert.equal(eligible(base,{day:'tue',date:'2026-09-22',time:'17:00'}),false)});
test('weighted selection respects weights',()=>{const b={...base,id:'b',weight:5};assert.equal(weightedPick([base,b],()=>.2,{day:'mon',date:'2026-09-21',time:'17:00'}).id,'b')});
test('recent slides are excluded when another is eligible',()=>{const b={...base,id:'b'},c={...base,id:'c'};assert.equal(weightedPick([base,b,c],()=>0,{day:'mon',date:'2026-09-21',time:'17:00'},['a','b']).id,'c')});
test('oldest exclusions relax while the previous slide stays blocked',()=>{const b={...base,id:'b'};assert.equal(weightedPick([base,b],()=>0,{day:'mon',date:'2026-09-21',time:'17:00'},['a','b']).id,'b')});
test('sole eligible slide can repeat',()=>assert.equal(weightedPick([base],()=>0,{day:'mon',date:'2026-09-21',time:'17:00'},['a']).id,'a'));
test('active event uses tagged slides and otherwise keeps them out of evergreen',()=>{const festive={...base,id:'festive',eventIds:['christmas']};assert.equal(weightedPick([base,festive],()=>0,{day:'mon',date:'2026-09-21',time:'17:00'},[],'christmas').id,'festive');assert.equal(weightedPick([base,festive],()=>0,{day:'mon',date:'2026-09-21',time:'17:00'}).id,'a')});
test('active event falls back to evergreen when no tagged slide is eligible',()=>{const festive={...base,id:'festive',eventIds:['christmas'],schedule:{tue:{enabled:true,start:'00:00',end:'23:59'}}};assert.equal(weightedPick([base,festive],()=>0,{day:'mon',date:'2026-09-21',time:'17:00'},[],'christmas').id,'a')});
test('validation rejects empty text',()=>assert.throws(()=>validateSlide({...base,headline:'',subheading:''})));
test('admin names follow headlines and number every duplicate',()=>{const slides=[validateSlide({...base,id:'new',name:'Anything',headline:'HELLO'}),validateSlide({...base,id:'old',name:'Old label',headline:'HELLO'}),validateSlide({...base,id:'other',headline:'WELCOME'})];assignAdminNames(slides);assert.deepEqual(slides.map(slide=>slide.name),['HELLO 2','HELLO 1','WELCOME'])});
test('Christmas countdown generates the correct singular and plural text',()=>{assert.deepEqual(christmasCountdownText('2026-11-30'),{headline:'25 SLEEPS',subheading:'UNTIL CHRISTMAS',sleeps:25});assert.deepEqual(christmasCountdownText('2026-12-24'),{headline:'1 SLEEP',subheading:'UNTIL CHRISTMAS',sleeps:1})});
test('Christmas countdown requires a chosen date window ending by Christmas Eve',()=>{assert.throws(()=>validateSlide({...base,type:'christmas-countdown',headline:'',starts:'',ends:''}));assert.throws(()=>validateSlide({...base,type:'christmas-countdown',headline:'',starts:'2026-12-01',ends:'2026-12-25'}));assert.doesNotThrow(()=>validateSlide({...base,type:'christmas-countdown',headline:'',starts:'2026-12-01',ends:'2026-12-24'}))});
test('Christmas countdown is rendered for the live screen',()=>{const slide=validateSlide({...base,type:'christmas-countdown',headline:'',starts:'2026-12-01',ends:'2026-12-24'});assert.equal(renderSlide(slide,{day:'thu',date:'2026-12-24',time:'12:00'}).headline,'1 SLEEP')});
